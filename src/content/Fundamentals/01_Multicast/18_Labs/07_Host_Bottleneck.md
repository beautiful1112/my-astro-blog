# Lab 7: Host bottleneck

Generate short high-pps bursts. Compare capture sequence, NIC queue counters, kernel UDP errors, socket drops, application sequences, and CPU scheduling.

Change ring size, socket buffer, interrupt moderation, and affinity one at a time.

