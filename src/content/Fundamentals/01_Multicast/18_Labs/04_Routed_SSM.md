# Lab 4: Routed SSM

Build a source, two PIM routers, and a receiver. Verify IGMPv3 INCLUDE reporting, `(S,G)` state without an RP, RPF at each hop, TTL decrement, and pruning after leave.

Then change the route to `S` so traffic arrives non-RPF and observe the failure counter.

